## 1. The payoff table has to agree with the mapping

- [x] 1.1 Implement the agreement check in `src/task/validate.ts` beside `checkPayoffs` — for every declared category, the entry for the action `categoryActions` maps it to must be strictly greater than every other declared action's entry in that row — refusing with a code of its own that names the category, its declared action and the action that matches or outpays it; run it only over rows the completeness check found complete, so one missing cell does not also report as a disagreement; verify tests cover an outpaid declared action, an exact tie, and that both refusals name all three ids
- [x] 1.2 Check the rule against what already ships: verify tests assert the apple declaration and the screening declaration in `test/validate-declaration.test.ts` and `web/src/test-support/declarations.ts` validate with no issues under it, and that `Game_design.md` §4.1's table — `wormy` mapped to discard at 0.00 while crating pays +0.40 — is refused naming `wormy`, so the rule is shown catching the table it was written for
- [x] 1.3 Confirm the rule does not over-reach: verify a test asserts a declaration carrying an action that no category maps to still validates, as long as no category's row pays that action at least as much as its own declared action
- [x] 1.4 Confirm the property the rule exists for: verify a test scores a small set of images under a validated declaration, gives every image the action its true category is declared to call for, brute-forces every other assignment of declared actions to those images, and asserts none earns more

## 2. Threshold policy resolves by declared priority

- [x] 2.1 Add the declared priority order to `ThresholdPolicy` in `src/task/types.ts`, documenting on the type why priority cannot be read off the declared category order — that order is bound to the artifact's probability-vector indexing by `prediction-artifacts`, so a task with shipped predictions could not change it; verify the existing threshold declarations in the test suite are updated to carry one rather than defaulted for
- [x] 2.2 Validate the priority order in `checkPolicy`: it must name every declared category exactly once, with an omission, a repeat and an undeclared id each refused naming the category at fault; verify one test per defect asserts the task does not run, and that a complete order is accepted
- [x] 2.3 Implement resolution in `chooseAction`'s threshold branch — among the categories meeting their thresholds, the one earliest in the declared priority order decides, and nothing meeting its threshold still falls back to the declared fallback action; verify tests assert that two categories clearing thresholds for different actions resolve by priority, that reversing the priority order reverses the chosen action with nothing else changed, that a category placed first on a low threshold outranks a more probable category clearing a higher one, and that the existing monotonicity property still holds with three actions in play
- [x] 2.4 Confirm the policy is still live over frozen data: verify a test changes only the priority order, evaluates the same images again, and asserts the chosen actions differ while the configuration identifier and the prediction artifact are untouched
- [x] 2.5 Confirm the other two policies needed nothing: verify tests assert highest-probability maps each of the three clear distributions to its category's action, that cost-optimal hedges into the cheaper crate at (0.45, 0.45, 0.10) where crating as green is worth +0.03 against −0.105 for crating as red and 0.00 for discarding, and that both tie-breaks still resolve to the earliest declared category and the earliest declared action

## 3. The apple task declares three actions

- [x] 3.1 Rewrite `declarations/apple-harvest.json`'s actions as `crate-red`, `crate-green` and `discard` with their declared labels, map `red → crate-red`, `green → crate-green`, `wormy → discard`, and carry the nine-cell payoff table `design.md` fixes; verify tests assert the three actions in declared order, the mapping, that all nine cells are numbers, and that the declaration validates with no issues
- [x] 3.2 Rewrite the declaration's task-level teaching copy, which is the only prose in the repository that spells out the two-action framing: say what the sorter does with an apple, what each of the two crating mistakes costs and what discarding costs, and keep `CLAUDE.md`'s honesty line by claiming nothing about training; verify tests assert both copy fields are present and non-empty and that the knob help copy is unchanged by this change
- [x] 3.3 Assert the numbers against the economy they came from: verify a test computes per-apple earnings from the declared table under `Game_design.md` §4.5's 55/35/10 crop mix and asserts perfect play is 0.29, that always-crate-as-red, always-crate-as-green and always-discard each earn strictly less, and that the worst blanket strategy is a loss

## 4. The tests written against two actions

- [x] 4.1 Rewrite `test/apple-harvest.declaration.test.ts`: nine cells rather than six, crating a wormy apple as the worst-paying outcome in the table and equally bad in either crate, and crating a red apple as red as the best-paying cell in its row; verify the file names no action id that the declaration no longer declares
- [x] 4.2 Rewrite `test/policy.test.ts` against the new ids and the new table, recomputing every expected-payoff figure quoted in a comment rather than leaving arithmetic that no longer holds; keep the flat-table tie-break case but make it read as a hand-built value for a determinism test rather than as a declaration that would validate, since the agreement rule refuses it
- [x] 4.3 Update the declarations built inside `test/artifact-contract.test.ts` and `test/artifact-index.test.ts` to the new action ids and to a threshold policy carrying a priority order; verify `test/diagram-resolve.test.ts`'s two-action toy task is left alone and still validates, since a task declaring two actions must keep working
- [x] 4.4 Rewrite `web/src/screens/Report.test.tsx` over three actions: nine cells present including zeros, the count of red apples crated as green distinguishable from the count of red apples discarded, and the over-selective configuration still diagnosable from the table alone

## 5. The report, and the screens that should not have noticed

- [x] 5.1 Confirm `Report.tsx` renders the wider table with no change: verify tests assert one column per declared action in declared order, every label taken from the declaration, and zero-count cells rendered rather than omitted
- [x] 5.2 Keep the second test declaration at two actions on purpose, so the suite proves the screens assume neither two nor three; verify `web/src/no-task-specific-code.test.tsx` passes with the new ids in its vocabulary and that no screen source names one
- [x] 5.3 Confirm the widening reached the student's screen and not only the engine: verify a test renders a report for a configuration that crates wormy apples and asserts the two crating columns carry separate counts for the wormy row

## 6. Nothing else moved

- [x] 6.1 Assert the artifacts are untouched: verify a test asserts the shipped prediction index still resolves the shipped defaults to `blocks2-channels16-regularization1-dropout0` and that its configuration coverage is unchanged, and confirm by hand that `git status` reports no change under `artifacts/` or `pools/`
- [x] 6.2 Confirm no artifact could have needed to change: verify the existing assertions that a prediction entry naming a category as true or carrying a chosen action is refused still hold, since they are the reason an action rename costs nothing
- [x] 6.3 Run the full suite and `openspec validate three-action-sorting --strict` through the bundled Node, and record in the change any requirement the run showed to be stated more narrowly than the code now behaves
